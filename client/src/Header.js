import './stylesheets/Header.css';

const Header = (props) => {
    return (
        <div className="header">
            <span className="header__headline">
                Virtual All-Star Battle!
            </span>
        </div>
    );
}

export default Header;
